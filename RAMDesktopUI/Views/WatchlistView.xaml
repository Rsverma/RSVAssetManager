<hc:Window  xmlns:hc="https://handyorg.github.io/handycontrol" 
            x:Class="RAMDesktopUI.Views.WatchlistView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RAMDesktopUI.Views"
        xmlns:controls="clr-namespace:RAMDesktopUI.Controls"
        mc:Ignorable="d" ShowTitle="False"
        x:Name="Watchlist" Height="450" Width="800" WindowState="{Binding CurWindowState, Mode=TwoWay}">
    <hc:Window.NonClientAreaContent>
        <TextBlock Text="Watchlist" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </hc:Window.NonClientAreaContent>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <controls:RSVDataGrid x:Name="SymbolMarketData" Grid.Row="1" Grid.Column="1" Margin="0 10" CanUserAddRows="False" 
                  CanUserDeleteRows="False" IsReadOnly="True"
                  CanUserResizeRows="False" AutoGenerateColumns="True"
                  >
        </controls:RSVDataGrid>
    </Grid>
</hc:Window>
